version: '3'
services:
  db:
    image: postgres:16.2-alpine3.19
    env_file:
      - .env_db
    ports:
      - "5432:5432"
    volumes:
      - "./db/data:/var/lib/postgresql/data"
    restart: always
    networks:
      backend:
  server:
    build: server/
    command: python ./app/app.py
    ports:
      - 8000:8000
    restart: always
    networks:
      - backend
    depends_on:
      - db
  client:
    build: client/
    command: serve public -p 80
    ports:
      - 80:80
    depends_on:
      - server
    restart: always

networks:
  backend:
